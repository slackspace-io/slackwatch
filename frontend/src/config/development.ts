import type { Config } from "./types";

const config: Config = {
  // Ensure all required properties by Config are included
  baseURL: 'http://localhost:8080/', // Example baseURL, adjust as needed
  // Add other properties required by Config
};

export default config;
